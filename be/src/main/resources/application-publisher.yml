spring:
  config:
    activate:
      on-profile: publisher

  main:
    web-application-type: none

  kafka:
    listener:
      auto-startup: false

  sql:
    init:
      mode: never

server:
  port: 0

outbox:
  publisher:
    enabled: true
